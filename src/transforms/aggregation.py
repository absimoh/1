# Grouping, averaging ratings, etc.
# Data aggregation layer

import logging, os
import apache_beam as beam

logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(levelname)s - %(message)s")


 